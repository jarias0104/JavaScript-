//this program should calculate the following currencies:
//dominican peso to usd, canadian dollar to usd and brazilian reais to usd.
//use only logical operators

const domPeso = 200;
const canDollar = 30;
const braReal = 100;

//the real market value will probably change, this is just a use of the daily exchange which is not permanent

const domToUsd = domPeso * 0.017;
const canToUsd = canDollar * 0.73;
const braToUsd = braReal * 0.18;

//print result in console.


console.log("Here is the result of your currencies.","200 Dominican Peso to USD:", domToUsd,"30 Canadian Dollar to USD:",canToUsd,"100 Brazilian Real to USD:",braToUsd);